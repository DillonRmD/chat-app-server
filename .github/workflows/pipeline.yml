name: CI/CD
on:
  push:
    branches: [ prod ]
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v2
      - name: Install Bun
        run: curl -fsSL https://bun.sh/install | bash
      - name: Run Tests
        run: bun test
